# SteelIQ – Lateral Enterprises

## Overview
SteelIQ is a professional quotation and estimating platform for the window and door industry. Its primary goal is to streamline the entire quotation process, from configuration and visualization to pricing and export, by offering a robust system for configuring items, generating live SVG technical drawings, and managing estimates and quotes. Key capabilities include real-time drawing previews, comprehensive estimate and quote lifecycle management, detailed pricing breakdowns, and a template-driven PDF engine for professional document generation. The platform aims to be a leading solution for efficient and accurate quoting within the industry.

## User Preferences
I want iterative development.
Ask before making major changes.
I prefer detailed explanations.
Do not make changes to the folder `shared` EXCEPT shared/schema.ts and shared/estimate-snapshot.ts (approved).

## System Architecture
**Frontend**: React, TypeScript, Shadcn UI for a responsive, mobile-first design.
**Backend**: Express.js with PostgreSQL, Drizzle ORM, and node-postgres.
**Drawing Engine**: SVG for rendering with real-time previews and PNG export.
**State Management**: React state for UI components; TanStack Query for API data.
**Global Settings**: Managed via React Context and localStorage.
**Routing**: Wouter for client-side navigation.
**Export Capabilities**: SVG to PNG conversion and multi-page, vector-text selectable PDF generation using jsPDF.
**Storage**: Item photos in PostgreSQL (`bytea`) with in-memory cache; drawing PNGs on filesystem. Division logos are stored as base64 data URLs in PostgreSQL.
**Multi-Division Architecture**: Supports organizational and division-specific settings, including `division_scope` for library entries.
**Spec Dictionary System**: Configurable `spec_dictionary` entries for dynamic specification display and override.
**Quote Management**: Comprehensive lifecycle (Draft, Review, Sent, Accepted/Declined, Archived), atomic sequential numbering, immutable revision history, and server-side status transition enforcement. Quotes include an `EstimateSnapshot` for immutable revision data.
**Pricing System**: Utility calculates material, labor, glass, liner, and handle costs, providing net cost, sale price, and margin, including configurable GST. Features manual price override and includes removal/rubbish fees.
**Configuration & Drawing**: Dedicated tables for frame configurations, profiles, accessories, and labor, with auto-detection of configurations, standard frame sizes, and dynamic opening indicators. Supports various window types, including French windows and hardened stacker door logic.
**Master Library Systems**: Centralized libraries for direct materials, manufacturing labor, installation labor, and delivery methods.
**Site Visit Mode**: Client-only `siteType` state for jobs to apply "renovation" and "new_build" defaults.
**Mobile Architecture**: Optimized with `native-scroll`, sticky action bars, enhanced item cards, and a collapsible header.
**Quote Document Model**: `PreviewData` (API response), `QuoteDocumentModel` (normalized rendering contract), and `buildQuoteDocumentModel()` (mapper).
**Quote Renderer**: `QuoteRenderModel` (presentation-ready structure) and `buildQuoteRenderModel()` (pure mapper from `QuoteDocumentModel`) for preview page rendering.
**Lifecycle Service**: Centralized handling for archive, soft-delete, hard-delete, cascade operations, orphan detection, and development cleanup routines. Template-driven, read-only lifecycle visibility for all quotes and operational jobs, with template locking upon quote acceptance.
**CRM Workflow (Projects-First)**: Project-centric CRM with automatic customer linking from quotes, project creation linked to quotes, and sub-query endpoints for project-related data (quotes, jobs, invoices). Enforces customer linkage for invoicing.
**Invoice Allocation Control**: Prevents over-invoicing with deposit caps (50% of accepted value, base contract only) and invoiceable ceiling caps. The invoiceable ceiling = accepted contract value + total approved variation amounts. Provides a detailed invoice allocation state via API including variation breakdown.
**Variations Commercial Model**: Project-level variations table (`draft → sent → approved → invoiced`). Variations link to both `projectId` and `quoteId`. Approved variations expand the invoiceable ceiling on the linked quote. Variation invoices must be linked to a specific approved variation record and cannot exceed its remaining value. The `getVariationsForAllocation` storage method fetches variations by quoteId plus project-level variations (no quoteId), ensuring backward compatibility.
**Retention Commercial Model**: Optional percentage-based retention withheld from the base contract only (not variations). `retentionPercentage` and `retentionHeldValue` stored on the `quotes` table. Standard invoice ceiling = `(acceptedValue − retentionHeld) + approvedVariations`. Retention released via a dedicated `retention_release` invoice type, capped at remaining unreleased retention. Configured via `PATCH /api/quotes/:id/retention`. Allocation endpoint returns full retention breakdown: `retentionConfigured`, `retentionPercentage`, `retentionHeldValue`, `retentionReleasedExcl`, `retentionRemainingExcl`, `standardInvoicedExcl`, `standardRemainingExcl`. UI: billing summary panel shows retention breakdown with amber highlight; "Set/Edit Retention" dialog; retention_release invoice type button appears only when retention remaining > 0; final invoice auto-suggests `standardRemainingExcl` with note to release retention separately.
**UI/UX Enhancements**: Improved table responsiveness for quotes and invoices lists, collapsing secondary columns on smaller viewports. PDF generation includes polished spec row formatting to handle wrapped lines cleanly. Project dashboard includes guidance text and links to related entities. Customer relink safeguard with confirmation dialog if invoices exist. Measurement logic on `op_jobs` table for `measurementRequirement` and `dimensionSource`.
**Detail Page Commercial Standardization**: Quote detail shows the full 9-row commercial hierarchy (Base Contract → +Variations → =Total Contract → −Retention → Std.Invoiced → +RetentionReleased → =TotalInvoiced → Std.Remaining). Color-coded invoice type pill badges (blue=deposit, violet=progress, indigo=variation, green=final, amber=retention_release, red=credit_note) consistent across quote-detail, project-detail, and op-job-detail surfaces. Retention-release invoices show "↳ Release of retained funds" context. Project detail summary shows 6-card commercial overview (Accepted Contract, Approved Variations, Invoiced, Outstanding) with corrected excl. GST outstanding calculation. Job detail Source Quote card shows accepted value and "Manage billing →" nav link.
**Enterprise App Shell / Navigation Architecture**: The sidebar is restructured into 6 workflow-domain groups using `SidebarGroupLabel`: Sales (Estimates, Quotes), Delivery (Projects, Jobs), Finance (Invoices), Master Data (Library, Customers, Contacts), Platform Roadmap (Scheduling/Procurement/Manufacture/Dispatch/Closeout/Reporting — all disabled placeholders), System (Users, Settings). A "+ New" quick-access dropdown is added to the top header bar with domain-grouped quick actions: New Estimate (→ /job/new), New Project (→ /projects), New Job (→ /op-jobs), New Invoice (→ /invoices). All existing routes remain intact. No architecture changes — sidebar restructure only.
**UI Standardization Phase 1 (Page Shell & Layout)**: Implemented Workstreams A–D from the 2nd control header. (A) Quote Builder: `itemsExpanded` now defaults to `true` — the item schedule panel starts at full height (55dvh) on desktop instead of the stub 160px, preserving collapse/expand toggle. (B) Estimates (`jobs-list.tsx`): converted from soft card grid to a proper operational table with columns Name / Address / Date / Items / m² / Actions (Open, Archive, Delete). Active and Archived tabs both use the table layout. (C) Op-Jobs list: removed `max-w-5xl mx-auto` constraint; applied full-width Library shell. (D) Projects, Invoices, Customers, Contacts: all migrated from `p-4 sm:p-6` floating layout to the Library benchmark shell (`flex flex-col h-full bg-background` + sticky `border-b px-4 sm:px-6 py-3` header + `flex-1 overflow-auto` body). Each header now carries a colored 8×8 icon badge, page title, subtitle, and right-aligned actions/search. No schema changes, no API changes, no architecture drift.
**UI/UX Hardening Phase 2 (Workflow Honesty + Estimator Polish + Data-State Clarity)**: Implemented all 5 phases (A–E) from the Phase 2 control header. (A) Global + New menu pruned: removed misleading "New Project", "New Job", "New Invoice" shortcuts — only "New Estimate", "New Customer", "New Contact" remain as valid global-create actions. File: `client/src/App.tsx`. (B) Quote Builder desktop polish: items panel heading renamed to "Quote Schedule" with `border-b bg-muted/30` treatment; secondary metrics (item count, total m², excl. price) shown as muted text; Collapse/Expand toggle has text labels (not icon-only); visual separator (`w-px h-5 bg-border`) added before Save button in the action bar. File: `client/src/pages/quote-builder.tsx`. (C) Invoice unlinked-state clarity: missing-customer rows show amber "Unlinked" badge + "Link customer →" repair link to the source quote; missing-project rows show italic "No project" + "Review record →" link — no more silent blanks. File: `client/src/pages/invoices.tsx`. (D) Settings + Users shell standardization: Users page migrated to Library benchmark shell with sticky header (UsersIcon badge, "User Management" title, "Role Guide" + "Add User" action buttons). Settings already had library shell; no change needed. File: `client/src/pages/users.tsx`. (E) Enterprise consistency pass: `quotes-list.tsx` migrated to Library shell (BookOpen icon badge, "Quotes" title, "All formal quotes across divisions" subtitle). Confirmed all touched pages (Estimates/Quotes/Projects/Jobs/Invoices/Customers/Contacts/Settings/Users) share the same consistent header pattern. All TC-001 through TC-006 passed end-to-end.
**Environment Clarity + Test Data Governance (3-phase spec, all complete)**: (Phase 1) Replaced misleading system-mode selector in Settings with truthful environment info panel; removed all `requireNonProductionMode` guards — governance is role-gated only (Owner/Admin), never environment-gated. (Phase 2) True invoice archive: `archivedAt` column on `invoices` table; `archiveInvoice()` storage method; all invoice list queries filter `WHERE archived_at IS NULL`; estimate/op-job/project delete routes enforce downstream safety checks with structured error codes (`LINKED_QUOTES_DOWNSTREAM`, `XERO_LINKED_DOWNSTREAM`, `LINKED_INVOICES_DOWNSTREAM`, `LINKED_RECORDS_DOWNSTREAM`); governance summary enriched with full chain context for all 5 entity types; frontend `GovernanceEntitySection` shows downstream warnings per entity. (Phase 3 CRM Governance) `isDemoRecord boolean` added to `customers` and `customer_contacts` (DB migrated); storage methods added: `deleteCustomer`, `getDemoCustomers`, `updateCustomerDemoFlag`, `getQuotesByCustomer`, `getJobsByCustomer`, `getInvoicesByCustomer`, `deleteContact`, `getDemoContacts`, `updateContactDemoFlag`, `getJobsByContact`, `archiveContact`, `archiveCustomer`, `getProjectsByCustomer`; PATCH `/api/customers/:id/demo-flag` and PATCH `/api/customer-contacts/:id/demo-flag` flag endpoints added (Owner/Admin, audit logged); governance summary extended to include `customers` and `contacts` arrays with full isolation analysis (`_isolation` object: `isSharedWithLiveData`, `xeroLinked`, per-entity live-record counts, `safeToArchive`, `safeToDelete`); governance archive and delete routes handle `customer` and `contact` entity types with Xero-link and live-data blocking; op-job delete/archive uses Phase B dual-path invoice detection (quote chain + project link, deduplicated); frontend governance panel shows "Customers (CRM)" and "Contacts (CRM)" sections with isolation status display.
**UI/UX Hardening Phase 3 (Interior Page Maturity + Enterprise Density)**: Applied enterprise table density improvements across 8 pages. (1) Invoices: all `<TableHead>` cells now carry `text-xs font-semibold uppercase tracking-wider text-muted-foreground`; Type column replaced plain text with a colored `<span>` pill badge (blue=deposit, violet=progress, indigo=variation, green=final, amber=retention_release, red=credit_note); Excl. GST column uses `font-mono font-semibold tabular-nums`; all cells carry `py-3`; customer/project cells wrapped in `<span className="font-medium">`. (2) Projects-list: headers upgraded to `font-semibold uppercase tracking-wider`; customer cell changed from `text-muted-foreground` to `font-medium` (primary identity); financial columns upgraded to `font-mono text-sm font-semibold tabular-nums`; Open button now has text + icon. (3) Op-Jobs-list: uppercase tracking headers; Source Quote column now resolves the actual quote number from a `quoteNumberMap` (fetches `/api/quotes`); `quoteNumberMap` passed as prop to `JobsTable` subcomponent; column hidden on small viewports; all data cells `py-3`. (4) Customers: added search input to page header with `customerSearch` state + `.filter()` on the rendered list; table headers upgraded; each customer row shows a `w-7 h-7 rounded-full bg-primary/10` avatar circle with first letter; cells carry `py-3`. (5) Contacts: table headers upgraded to uppercase tracking; name column wraps avatar circle (`initials` computed from firstName + lastName) plus name + role title stack; cells carry `py-3`; map changed from arrow-return to block-return syntax. (6) Settings: `max-w-2xl` widened to `max-w-3xl`. (7) Users: all `<TableHead>` cells upgraded to uppercase tracking; all `<TableCell>` cells carry `py-3`; rows carry `hover:bg-muted/30`. (8) Quote Builder: center workspace background changed from hardcoded `linear-gradient` to `bg-muted/30 dark:bg-muted/10` (theme-aware); canvas wrapper upgraded with `rounded-lg overflow-hidden shadow-sm ring-1 ring-border/50 bg-background` for a framed-card appearance. All 8 TCs passed end-to-end.

## External Dependencies
- **PostgreSQL**: Primary database.
- **Drizzle ORM**: ORM for PostgreSQL.
- **node-postgres**: PostgreSQL client.
- **React**: Frontend UI library.
- **TypeScript**: For static typing.
- **Shadcn UI**: Component library.
- **TanStack Query**: Data fetching and caching.
- **Wouter**: Client-side routing.
- **jsPDF**: PDF generation.
- **multer**: File uploads.