# SteelIQ – Lateral Enterprises

## Current Milestone
Quote Layout & Pagination Pass (T146–T151) complete. Profile Role Dictionary & Reference Images (T139–T145) complete. Lock Type Validation Pass (T131–T138) complete. Locks Hardware Category (T120–T129) complete. Library-Backed Preset Dropdowns (T113–T119) complete. Editable Job Type Presets (T107–T112) complete. Division Settings UX Cleanup (T101–T106) complete. Saved Config Calibration (T096–T100) complete. Default Tuning Pass (T090–T095) complete. Template Builder v1.3 (T083–T089) complete. Company Master Template v2 (T077–T082) complete.
- **Locks Hardware Category (T120–T129)**: Added Locks as a fully library-backed hardware category alongside Handles. T120: LOCK_TYPES fallback defaults, LOCK_CATEGORIES allocation by door type (entrance/hinge/sliding/bifold/stacker/french), getLockTypeForCategory(), getLocksForCategory(), isDoorCategory() added to `shared/item-options.ts`. T121: Library "Handles" tab renamed "Hardware" with collapsible Handles + Locks sections; LockCategoryCollapsible component added to `library.tsx`. T122: `lockType: z.string().default("")` added to quoteItemSchema. T123: Lock Type `<Select>` added to Quote Builder Hardware section (door categories only), library-backed with category-specific lock queries. T124: `lockPriceEach` added to PricingExtras, `lockCostNzd` to pricing calculation; Customer-Supplied/TBC → $0. T125: `lockType` added to SiteVisitPresetDefaults, PRESET_FIELD_LABELS, preset editor in settings, useLibraryOptions hook. T126: `lockSet` added to specValues/resolvedSpecs in exec-summary snapshot builder; `lockSet` spec dictionary entry added (Hardware group); added to LJ_DEFAULT_SPEC_DISPLAY_KEYS. T127: Lock exclusion notes added to snapshot — Customer-Supplied/TBC/empty locks generate exclusion entries. T128: Lock seed defaults added to seedLibraryDefaults; presetDefaultsSchema updated with lockType field; Zod validation for lock presets. T129: scope guard preserved — no template/lifecycle/renderer changes beyond lockSet spec display.
- **Library-Backed Preset Dropdowns (T113–T119)**: Replaced free-text preset inputs with constrained Select dropdowns backed by actual Quote Builder option sources. T113: created `useLibraryOptions` hook (`client/src/hooks/use-library-options.ts`) providing same option sources as Quote Builder — frameType from library frame_type entries, glassIguType static (EnergySaver/LightBridge), glassType/glassThickness cascading from glass library, linerType from library, handleType from library, windZone from WIND_ZONES static array. T114: PresetFieldEditor rewritten with `PresetSelectField` component; all fields except wallThickness (numeric input) are now constrained Select dropdowns. T115: invalid/outdated saved values detected and shown with amber AlertTriangle warning prompting admin to update. T116: cascading resets — changing glassIguType clears glassType and glassThickness; changing glassType clears glassThickness. T117: Quote Builder `handleSiteTypeChange` now always merges preset overrides onto current form values at selection time (not gated by dirty/editing state), preserving manual edits afterward. T118: scope guard preserved — no schema/API/lifecycle changes. T119: architecture separation maintained.
- **Editable Job Type Presets (T107–T112)**: Division job type presets (Renovation/New Build) are now editable, persisted, and used by Quote Builder. T107: added `job_type_presets_json` JSONB column to `division_settings`. T108: settings UI replaced read-only presets with editable form fields for all 8 preset fields (frameType, glassIguType, glassType, glassThickness, linerType, handleType, wallThickness, windZone) per job type. T109: presets save/load through existing PATCH `/api/settings/divisions/:code`; Zod validation added with `presetDefaultsSchema` and `jobTypePresetsSchema` (strict shape). Fallback: `resolvePresetsForDivision` deep-merges persisted values onto SEED_PRESETS so partial data is safe. T110: Quote Builder `getPresetDefaults()` now reads from persisted division presets via `divisionPresets` (resolved from `divisionSettings.jobTypePresetsJson`). T111: estimate defaults remain fully separate from quote presentation system. T112: scope guard preserved.
- **Division Settings UX Cleanup (T101–T106)**: Division page reorganized into two sections: (1) Quote Presentation, (2) On-Site Estimate Defaults. Locked template replaced with informational display.
- **Saved Config Calibration (T096–T100)**: Audited and calibrated the actual persisted company template config. T096: audit revealed saved config was sparse (only logoScale, documentMode, densityPreset, showTradingName) — granular fields missing, relying on code defaults. T097: saved full calibrated baseline with 14 explicit values: `logoScale=large`, `logoWidthMm=70`, `logoMaxHeightMm=26`, `showTradingName=false`, `legalLinePlacement=under_logo`, `contactBlockAlignment=compact_right`, `headerBottomSpacingMm=5`, `densityPreset=standard`, `drawingMaxHeightMm=40`, `photoMaxHeightMm=20`, `specRowHeightMm=3.3`, `itemCardPaddingMm=2.8`, `itemCardGapMm=2.8`, `documentMode=tender`. T098: validated on representative quote — Preview and PDF both reflect saved config correctly. T099: builder hydration hardened to always reflect persisted config (removed stale `Object.keys` guard); builder/saved/render agreement confirmed. T100: scope guard preserved — no new systems, endpoints, or schema changes.
- **Default Tuning Pass (T090–T095)**: Company master template defaults tuned for stronger out-of-box professional output. Header defaults, schedule density, visual hierarchy (photos de-emphasized to 20mm, drawings primary). Photo presets recalibrated (15/20/35mm). Builder select fallbacks use COMPANY_MASTER_TEMPLATE constants.
- **Template Builder v1.3 (T083–T089)**: Granular template controls + A4 lock + header/schedule refinement. Strict A4 geometry, granular header controls (logoWidthMm, logoMaxHeightMm, legalLinePlacement, contactBlockAlignment, headerBottomSpacingMm), granular schedule controls (drawingMaxHeightMm, photoMaxHeightMm, specRowHeightMm, itemCardPaddingMm, itemCardGapMm). All controls affect both Preview and PDF. Server schema extended with bounded Zod validation.
- **Company Master Template v2 (T077–T082)**: Professional redesign of the default company quote template. Header redesigned: logo is now the primary brand element (default `large`), trading name off by default (`showTradingName: false`), legal line at 6.5pt/10px italic, logo+text placed side-by-side instead of stacked. Schedule density tightened to professional defaults targeting ~4 items/page: `drawingMaxH=32`, `specRowH=3.8`, `itemHeaderH=10`, `photoRowH=22`, `itemCardPadMm=3`, `itemGapMm=3`. Density presets recalibrated: comfortable(~3/page), standard(~4/page), compact(~5/page). Item card composition refined: single-line header with title left + qty/dims right, tighter padding, cleaner visual hierarchy, smaller photo label text. Photo size presets tightened (18/25/40mm). Spacing presets tightened (compact=3mm, standard=5mm, spacious=8mm). Builder controls relabeled: "Logo Scale" with "(recommended)" on large, "Show Company Name" with guidance about logo-contains-name, density options show items/page estimates. All changes applied to both Preview and PDF with aligned composition logic. Template ID updated to `company_master_v2`. Architecture guards preserved — no schema/API/lifecycle changes.
- **Template Builder v1 (T056–T062)**: Structured config editor for the company master quote template. Persisted via `org_settings.template_config_json` (JSONB). `CompanyTemplateConfig` type defines safe editable fields: sections (visibility toggles), spacingPreset (compact/standard/spacious), typographyPreset (small/standard/large), photoSizePreset (small/medium/large), accentColor, scheduleLayoutVariant, totalsLayoutVariant. Resolution chain: hardcoded COMPANY_MASTER_TEMPLATE defaults → company config (`applyCompanyConfig`) → division overrides (`resolveQuoteTemplate`). Server: `GET/PATCH /api/settings/template` with Zod validation. Preview-data endpoint includes `companyTemplateConfig`. Settings page has new "Template" tab with builder UI: Sections card (7 section visibility toggles), Summary card (totals layout), Schedule card (layout + photo size), Theme/Spacing card (accent color picker, typography/spacing presets). Live schematic preview panel shows section layout, accent color, spacing/type labels. Division settings relabeled "Division Override" with clear reference to Template tab. No drag-and-drop, no freeform editing, no per-customer templates. Architecture guards preserved.

## Overview
SteelIQ is a professional quotation and estimating platform designed for Lateral Enterprises, catering to the window and door industry. It provides a robust, user-friendly system for configuring window and door items, generating live SVG technical drawings, and managing estimates and quotes. The platform streamlines the entire quotation process from configuration and visualization to pricing and export, ensuring accurate and visually rich outputs. Key capabilities include real-time drawing previews, comprehensive estimate and quote lifecycle management, item photo capture, detailed pricing breakdowns, and a template-driven PDF engine for professional document generation.

## User Preferences
I want iterative development.
Ask before making major changes.
I prefer detailed explanations.
Do not make changes to the folder `shared` EXCEPT shared/schema.ts and shared/estimate-snapshot.ts (approved).

## System Architecture
**Frontend**: React with TypeScript and Shadcn UI components, emphasizing a responsive mobile-first design.
**Backend**: Express.js with PostgreSQL, Drizzle ORM, and node-postgres.
**Drawing Engine**: SVG-based rendering with PNG export capabilities, integrated with real-time drawing previews.
**State Management**: React state for UI components and TanStack Query for API data fetching and caching.
**Global Settings**: Managed via React Context and persisted in localStorage.
**Routing**: Wouter for client-side navigation.
**Export Capabilities**: SVG to PNG conversion and multi-page, vector-text selectable PDF generation using jsPDF.
**Storage**: Item photos are stored in PostgreSQL (`bytea` column) with an in-memory cache. Drawing PNGs are stored on the filesystem.
**Multi-Division Architecture**: Supports organizational and division-specific settings, including `division_scope` for library entries.
**Spec Dictionary System**: Configurable `spec_dictionary` entries for dynamic specification display and override functionality.
**Quote Management**: Features a comprehensive lifecycle (Draft, Review, Sent, Accepted/Declined, Archived), atomic sequential numbering, immutable revision history, and server-side status transition enforcement. Quotes include an `EstimateSnapshot` for immutable revision data.
**Pricing System**: A utility calculates material, labor, glass, liner, and handle costs, providing net cost, sale price, and margin, including configurable GST.
**Configuration & Drawing**: Utilizes dedicated tables for frame configurations, profiles, accessories, and labor, with features like auto-detection of configurations, standard frame sizes, and dynamic opening indicators.
**Master Library Systems**: Centralized libraries for direct materials, manufacturing labor, installation labor, and delivery methods.
**Site Visit Mode**: Client-only `siteType` state for jobs, enabling preset defaults for "renovation" and "new_build" contexts.
**Mobile Architecture**: Optimized for mobile with `native-scroll`, sticky action bars, enhanced item cards, and a collapsible header.
**Quote Document Model**: Defines `PreviewData` (API response shape), `QuoteDocumentModel` (normalized rendering contract), and `buildQuoteDocumentModel()` (mapper).
**Quote Renderer**: Defines `QuoteRenderModel` (presentation-ready structure) and `buildQuoteRenderModel()` (pure mapper from `QuoteDocumentModel`). The preview page renders from `QuoteRenderModel` via decomposed section components.
**Lifecycle Service**: Centralized handling for archive, soft-delete, hard-delete, cascade operations, orphan detection, and development cleanup routines.

## External Dependencies
- **PostgreSQL**: Primary database.
- **Drizzle ORM**: Object-Relational Mapper for PostgreSQL.
- **node-postgres**: PostgreSQL client for Node.js.
- **React**: Frontend UI library.
- **TypeScript**: Superset of JavaScript for static typing.
- **Shadcn UI**: Component library for React.
- **TanStack Query**: Data fetching and caching library.
- **Wouter**: Client-side routing library.
- **jsPDF**: JavaScript library for generating PDFs.
- **multer**: Middleware for handling `multipart/form-data` for file uploads.