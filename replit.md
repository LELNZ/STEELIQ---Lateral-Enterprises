# SteelIQ – Lateral Enterprises

## Overview
SteelIQ is a professional quotation and estimating platform tailored for the window and door industry. It offers a robust and user-friendly system for configuring window and door items, generating live SVG technical drawings, and managing estimates and quotes. The platform's primary goal is to streamline the entire quotation process, from configuration and visualization to pricing and export, ensuring accurate and visually rich outputs. Key features include real-time drawing previews, comprehensive estimate and quote lifecycle management, item photo capture, detailed pricing breakdowns, and a template-driven PDF engine for professional document generation. The project aims to be a leading solution for efficient and accurate quoting within the industry.

## User Preferences
I want iterative development.
Ask before making major changes.
I prefer detailed explanations.
Do not make changes to the folder `shared` EXCEPT shared/schema.ts and shared/estimate-snapshot.ts (approved).

## System Architecture
**Frontend**: Built with React, TypeScript, and Shadcn UI components, emphasizing a responsive mobile-first design.
**Backend**: Implemented using Express.js with PostgreSQL, Drizzle ORM, and node-postgres.
**Drawing Engine**: Utilizes SVG for rendering with PNG export capabilities and integrated real-time drawing previews.
**State Management**: React state manages UI components, while TanStack Query handles API data fetching and caching.
**Global Settings**: Managed via React Context and persisted in localStorage.
**Routing**: Wouter is used for client-side navigation.
**Export Capabilities**: Supports SVG to PNG conversion and multi-page, vector-text selectable PDF generation using jsPDF.
**Storage**: Item photos are stored in PostgreSQL (`bytea` column) with an in-memory cache, and drawing PNGs are stored on the filesystem.
**Multi-Division Architecture**: The system supports organizational and division-specific settings, including `division_scope` for library entries.
**Spec Dictionary System**: Features configurable `spec_dictionary` entries for dynamic specification display and override functionality.
**Quote Management**: Includes a comprehensive lifecycle (Draft, Review, Sent, Accepted/Declined, Archived), atomic sequential numbering, immutable revision history, and server-side status transition enforcement. Quotes include an `EstimateSnapshot` for immutable revision data.
**Pricing System**: A utility calculates material, labor, glass, liner, and handle costs, providing net cost, sale price, and margin, including configurable GST.
**Configuration & Drawing**: Utilizes dedicated tables for frame configurations, profiles, accessories, and labor, with features like auto-detection of configurations, standard frame sizes, and dynamic opening indicators.
**Master Library Systems**: Centralized libraries are maintained for direct materials, manufacturing labor, installation labor, and delivery methods.
**Site Visit Mode**: A client-only `siteType` state for jobs enables preset defaults for "renovation" and "new_build" contexts.
**Mobile Architecture**: Optimized for mobile with `native-scroll`, sticky action bars, enhanced item cards, and a collapsible header.
**Quote Document Model**: Defines `PreviewData` (API response shape), `QuoteDocumentModel` (normalized rendering contract), and `buildQuoteDocumentModel()` (mapper).
**Quote Renderer**: Defines `QuoteRenderModel` (presentation-ready structure) and `buildQuoteRenderModel()` (pure mapper from `QuoteDocumentModel`). The preview page renders from `QuoteRenderModel` via decomposed section components.
**Lifecycle Service**: Centralized handling for archive, soft-delete, hard-delete, cascade operations, orphan detection, and development cleanup routines.
**Phase 7 Features**: (1) French window types — `windowType` enum includes `french-left`, `french-right`, `french-pair`; config-signature and drawing canvas handle all three. (2) Manual price override — `overrideMode` (`none`|`per_sqm`|`total_sell`) and `overrideValue` in `quoteItemSchema`; override UI in Cost Breakdown panel with mode select, value input, and clear button; applies override in pricing calculation via `salePriceOverride`. (3) Old window/door removal fee — `removalEnabled`, `removalOverride`, `removalMarkup` on `jobs` table; per-unit rate table from `removal_rate` library; full section in exec-summary with override input. (4) Rubbish/general waste removal — `rubbishEnabled`, `rubbishTonnage` on `jobs` table; per-tonne rate from `general_waste` library; full section in exec-summary. Both removal and rubbish included in grand total and `EstimateSnapshot.totalsBreakdown`.
**Stacker Door Hardening (Phase 6 follow-up)**: `entranceDoorRowSchema` extended to include `type: "sliding"` and `slideDirection: "left"|"right"`. `deriveConfigSignature` has a dedicated stacker-door branch that reads actual `panelRows[i][j].type` counts (awning/fixed/sliding) instead of estimating from panel count. `getLayoutSummary` for stacker shows row type composition: "N Panels (X SLD + Y AWN + Z FIX)". Stacker config signature prefix changed from `sliding:` to `stacker:` for correct library matching.
**CRM Workflow Rebuild (Projects-First Flow)**: Full project-centric CRM workflow implemented. Key features: (1) `POST /api/quotes` auto-carries customer from source estimate (match/create by name/email/phone, back-links `customerId` to job). (2) `PATCH /api/invoices/:id` blocks `ready_for_xero` with 422 + `{ error, code:"MISSING_CUSTOMER", quoteId }` when quote has no linked customer. (3) `POST /api/quotes/:id/create-project` creates project linked to quote (requires `customerId` on quote), prefills name/address from source job. (4) Sub-query endpoints: `GET /api/projects/:id/quotes|jobs|invoices`. (5) Projects pages at `/projects` (list with search, counts, totals) and `/projects/:id` (detail with collapsible inline Quotes/Jobs/Invoices sections). (6) Projects sidebar nav item. (7) Quote-detail restructured: "Customer-facing Details" and "Revisions" in collapsible cards (closed by default), "Related Quotes" in collapsible, lifecycle panel moved to bottom after accepted-state actions. (8) `CustomerProjectSection` adds "Create Project" button on accepted quotes with customer but no project; dialog prefills name from customer, address from source job. (9) `ConvertToJobSection` shows soft warning when no project linked. (10) Invoice MISSING_CUSTOMER error surfaces inline alert with link to quote (both on `/invoices` page and quote-detail InvoiceSection). Storage methods added: `getQuotesByProject`, `getOpJobsByProject`, `getInvoicesByProject`.

**Global Lifecycle Platform (Phase 1 + Foundation Tightening)**: Template-driven, read-only lifecycle visibility for all quotes and operational jobs. Key files: `shared/lifecycle.ts` (framework types), `server/lifecycle-templates.ts` (LJ template v1 + seeder), `server/lifecycle-service.ts` (derivation from system signals — accepts `template: LifecycleTemplateConfig` directly as parameter, no hardcoded constant), `client/src/components/lifecycle-panel.tsx` (UI). LJ division has 10 lifecycle stages (Estimate → Closeout) derived from quote status, invoice state, and opJob status. Template version locked at quote acceptance via `lifecycle_instances` table. `lifecycle_templates` and `lifecycle_instances` DB tables added (unique index on `lifecycle_instances.quote_id`). API: `GET /api/quotes/:id/lifecycle`, `GET /api/op-jobs/:id/lifecycle`. Template loading is now DB-driven: post-acceptance routes load by `instance.templateId` (locked version); pre-acceptance routes load the active template by division code. Blocked-state derivation: `site_measure` is blocked when `commercial_setup` is active (quote accepted, no processed invoice, no opJob). Panel shows three footer states: "Preview" (pre-acceptance), "Active template (accepted pre-tracking)" (accepted, no instance), "Template locked {date}" (accepted with locked instance). Panel shown on quote-detail and op-job-detail pages. Storage method `getLifecycleTemplateById` added.

**Invoice Allocation Control (Over-Invoicing Prevention)**: Commercially safe invoice creation enforced via two layers. (1) Deposit cap: max deposit = 50% of accepted contract value (excl. GST); checked at `POST /api/invoices` for deposit type — returns 422 `DEPOSIT_ALLOWANCE_EXHAUSTED` if fully used, `OVER_DEPOSIT_ALLOWANCE` if partially exceeded. (2) Contract value cap: total of all active invoices (draft, ready_for_xero, pushed_to_xero_draft, approved) cannot exceed acceptedValue — checked at `POST /api/invoices` and hard-blocked again at PATCH status→`ready_for_xero` gateway. `returned_to_draft` invoices excluded from allocation (retracted by operator). New endpoint `GET /api/quotes/:id/invoice-allocation` returns full allocation state: acceptedValueExcl/Incl, totalInvoicedExcl/Incl, remainingExcl/Incl, depositInvoicedExcl, depositAllowancePct/Excl/RemainingExcl, depositAllowanceFullyUsed. Frontend `InvoiceSection` in `quote-detail.tsx` overhauled: (a) allocation bar shows Contract Value / Total Invoiced / Remaining with live progress bar and deposit allowance status; (b) dialog expanded to deposit/progress/variation/final type selector; (c) deposit dialog shows allowance breakdown and red "fully used" warning + disables create button; (d) final invoice dialog pre-fills remaining balance; (e) all status-changing mutations invalidate the allocation cache. All 6 required TCs passed via API tests.

**Polish Pass — Phases A–D (Lifecycle Fix, Project Dashboard, Customer Safety, Measurement Logic)**:
- *Phase C (Lifecycle Collapse Fix)*: `lifecycle-panel.tsx` StageRow restructured — `TaskChecklist` moved OUTSIDE the clickable header `<div>` into its own wrapper div with `e.stopPropagation()`. Previously, clicking task checkboxes bubbled up to the stage header onClick and collapsed/expanded the stage unintentionally. Now clicking tasks has no effect on stage expand/collapse state. Vertical connector line between stages also adjusted to render correctly in both expanded and collapsed states.
- *Phase D (Project Dashboard Polish)*: `project-detail.tsx` — all three empty states (Quotes, Jobs, Invoices) now include guidance text explaining next steps. Quotes table gains an "Accepted" column showing `acceptedValue` for accepted quotes. `quote-detail.tsx` — header card strip now includes a "View Project" card (`link-header-project`) when `quote.projectId` is set. `op-job-detail.tsx` — project card in Source Relationships is now a clickable link (`link-detail-project`) navigating to `/projects/:id`.
- *Phase A (Customer Safety — Relink Safeguard)*: `CustomerProjectSection` in `quote-detail.tsx` now fetches the quote's invoices. When user clicks "Change" and existing invoices are present AND a customer/project is already linked, a warning `AlertDialog` is shown explaining that relinking will not update existing invoices and may cause Xero linkage drift. User must explicitly confirm before the edit form opens.
- *Phase B (Measurement Logic)*: `op_jobs` table gains two nullable text fields: `measurementRequirement` (values: `pre_quote | post_acceptance | not_required`) and `dimensionSource` (values: `site_measure | confirmed_drawings | client_supplied | engineer_drawings | architectural_drawings | other`). Schema types `MEASUREMENT_REQUIREMENTS` and `DIMENSION_SOURCES` exported from `shared/schema.ts`. `PATCH /api/op-jobs/:id` route schema updated to accept both fields. `lifecycle-service.ts` site_measure stage now returns `not_applicable` when `opJob.measurementRequirement === "not_required"`. `op-job-detail.tsx` exposes a "Measurement & Dimensions" section showing current values with a summary display, plus edit form selects for both fields in the edit panel.

## External Dependencies
- **PostgreSQL**: Primary database for the application.
- **Drizzle ORM**: Used for object-relational mapping with PostgreSQL.
- **node-postgres**: PostgreSQL client for Node.js.
- **React**: Frontend UI library.
- **TypeScript**: Used for static typing across the codebase.
- **Shadcn UI**: Component library for React.
- **TanStack Query**: For data fetching and caching.
- **Wouter**: Client-side routing library.
- **jsPDF**: JavaScript library for generating PDFs.
- **multer**: Middleware for handling `multipart/form-data` for file uploads.