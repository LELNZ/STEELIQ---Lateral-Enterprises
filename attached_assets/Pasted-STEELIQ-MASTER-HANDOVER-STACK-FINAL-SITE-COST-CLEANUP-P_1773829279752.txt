STEELIQ MASTER HANDOVER STACK — FINAL SITE COST CLEANUP PROMPT

AGENT CONTROL HEADER — MUST FOLLOW EXACTLY

You are working on an existing production-oriented system. You must implement exactly what is requested and preserve the current architecture.

Non-negotiable rules:
- Do not redesign the architecture.
- Do not change the quote lifecycle without explicit instruction.
- Do not replace or bypass the QuoteDocument pipeline.
- Do not merge snapshot state with live editable state.
- Do not introduce schema creep outside the requested phase.
- Do not add unapproved endpoints.
- Do not perform hidden refactors.
- Do not make speculative improvements that were not requested.
- Do not rename, relocate, or restructure major modules unless explicitly instructed.
- Do not “clean up” by changing architecture.
- Extend the current system incrementally and safely.
- If you believe a structural change is required, stop and explain why before making it.

==================================================
CURRENT TRUTH
==================================================

PR #14 improved the site-cost situation, but two issues remain:

1. Installation Labour footer now duplicates subtotal information:
- left side shows "Subtotal cost / Subtotal sell"
- right side also shows "Cost / Sell"
This is unnecessary duplication.

2. Removal and Waste are now surfaced in Library, but the structure is still not the requested unified Site Costs grouping.
Current state is still separate top-level tabs:
- Installation
- Delivery
- Removal
- Waste

User wants these grouped together as one coherent site-cost management area.

This phase is a small cleanup only.

==================================================
THIS PHASE — EXACT SCOPE
==================================================

Implement only these two items:

1. Remove duplicate subtotal display in Installation Labour footer
2. Reorganize site-cost library surfaces into one coherent Site Costs area

Do NOT broaden scope.

==================================================
GOAL 1 — REMOVE DUPLICATE INSTALLATION FOOTER SUMMARY
==================================================

Current issue:
- Installation Labour footer shows subtotal information twice
- left subtotal block duplicates the right-side Cost/Sell summary

Required result:
- remove the extra duplicate subtotal block on the left
- keep one clear summary only
- footer must remain truthful and uncluttered

Preferred result:
- retain the right-side Cost / Sell summary block
- remove the left-side duplicate subtotal text

==================================================
GOAL 2 — UNIFY SITE COST LIBRARY STRUCTURE
==================================================

Current issue:
- Installation / Delivery / Removal / Waste are separate top-level tabs
- this is better than hidden data, but still not the requested grouped site-cost structure

Required direction:
Create one coherent Site Costs area for staff to manage:
- Installation
- Delivery
- Removal
- Waste / Disposal

Best preferred implementation:
- a single top-level "Site Costs" tab
- with internal sub-tabs or sections for:
  - Installation
  - Delivery
  - Removal
  - Waste

If current library architecture cannot safely support a literal new parent tab:
- implement the closest safe equivalent
- but do NOT leave these as four separate top-level tabs if avoidable

Required result:
- staff can manage all site-cost rates from one clearly grouped place
- structure is cleaner and matches operational expectations

==================================================
REQUIRED REVIEW BEFORE IMPLEMENTING
==================================================

Before changing code:
1. Review current code as source of truth
2. Identify exact files involved
3. State where the duplicate installation subtotal block is rendered
4. State the safest way to unify the library into one Site Costs area
5. Then implement

==================================================
REGRESSION REQUIREMENTS
==================================================

Must verify:
- Installation footer no longer duplicates subtotal summary
- one clear footer summary remains
- Installation, Delivery, Removal, and Waste are all still editable
- Library remains division-safe
- no break to existing rate CRUD
- no break to quote builder / exec summary calculations

==================================================
MANDATORY HANDOVER FORMAT
==================================================

Return final handover in this exact structure:

1. Files changed
2. Planned tasks summary
3. Exact issues found
4. Behaviour changes
5. Schema/API changes
6. UI changes
7. Architecture verification
8. Risks/limitations
9. Updated current system state
10. Updated recommended next tasks
11. Regression checks

Do not claim completion unless both cleanup items are actually fixed and visually verified.