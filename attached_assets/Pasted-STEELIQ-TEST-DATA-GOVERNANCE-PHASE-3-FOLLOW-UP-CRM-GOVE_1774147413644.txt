STEELIQ TEST DATA GOVERNANCE — PHASE 3 FOLLOW-UP
CRM GOVERNANCE USABILITY + CONTACT ISOLATION TRUTH

AGENT CONTROL HEADER — MUST FOLLOW EXACTLY

You are continuing work on an existing live, production-oriented system. You must preserve the current architecture and close the remaining Phase 3 gaps only.

Non-negotiable rules:
- Do not redesign the architecture.
- Do not change the quote lifecycle.
- Do not replace or bypass the QuoteDocument -> QuoteRenderModel -> Preview/PDF pipeline.
- Do not merge immutable snapshot state with live editable state.
- Do not introduce speculative schema creep outside what is required to complete the listed functionality.
- Do not add unapproved endpoints outside what is needed for this follow-up.
- Do not perform hidden refactors.
- Do not make speculative improvements not requested.
- Do not weaken any live Xero/accounting safeguards.
- Do not broaden deletion capability.
- Do not mark this phase complete unless the required Owner/Admin UI surfaces are operationally usable.
- Do not claim completion from backend changes alone.
- Treat the current repository code and runtime truth as the implementation source of truth.
- Preserve the current governance architecture from Phases 1–3.
- Do not broaden scope into a new governance phase.

CURRENT CONTEXT

Phase 3 added:
- isDemoRecord on customers and contacts
- CRM governance archive/delete logic
- CRM isolation analysis in governance summary
- Customers and Contacts sections in governance review
- op-job dual-path invoice detection

Remaining accepted concerns from the formal report:
1. Contact isolation summary can be optimistic/inaccurate when the parent customer is not itself demo-flagged.
2. There is no usable Owner/Admin UI to flag customers and contacts as demo/test; only API endpoints exist.

PRIMARY OBJECTIVE

Close only the remaining Phase 3 usability/truth gaps:
1. make contact isolation summary truthful
2. add operationally usable Owner/Admin UI for customer/contact demo-flagging
3. keep all current enforcement and safety rules intact

Do not start Phase 4.
Do not broaden scope.

PHASE A — CONTACT ISOLATION SUMMARY TRUTH

OBJECTIVE

Ensure the governance summary and UI do not present a contact as isolated when the backend would actually block governance because the parent customer is shared with live operational data.

REQUIRED IMPLEMENTATION

1. Fix contact isolation summary logic so parent-customer live-data sharing is evaluated truthfully even when the parent customer is not itself demo-flagged.
2. The governance summary/UI must align with backend enforcement.
3. Do not leave an optimistic “safe to archive/delete” state in the UI when the backend will block the action.

ACCEPTANCE CRITERIA

- Contact isolation summary is truthful
- UI pre-warning matches backend safety outcome
- No misleading “isolated” display for shared-live contacts

PHASE B — OWNER/ADMIN UI FOR CUSTOMER/CONTACT DEMO FLAGGING

OBJECTIVE

Make customer/contact governance operationally usable without requiring direct API calls.

REQUIRED IMPLEMENTATION

1. Add usable Owner/Admin-only UI to mark/unmark customers as demo/test.
2. Add usable Owner/Admin-only UI to mark/unmark contacts as demo/test.
3. Use existing UI patterns and preserve current architecture.
4. Do not expose these controls to non-high-level users.
5. The UI does not need a broad redesign; use the smallest serious enterprise-safe surface that is operationally usable.

Acceptable surfaces include current customer/contact detail pages or existing admin/governance surfaces, whichever fits current architecture best.

Required behavior:
- clearly labeled as test/demo governance control
- role-gated to Owner/Admin
- visible and usable without direct API calls
- does not encourage accidental use by standard users

ACCEPTANCE CRITERIA

- Owner/Admin can flag/unflag customers in the UI
- Owner/Admin can flag/unflag contacts in the UI
- Non-high-level users cannot access these controls

PHASE C — PRESERVE CURRENT SAFETY MODEL

REQUIRED IMPLEMENTATION

Do not weaken:
- customer shared-live blocking
- contact shared-live blocking
- Xero protections
- op-job downstream finance protections
- role gating
- Settings > System truthfulness

STRICT NON-GOALS

Do NOT:
- redesign customer/contact pages broadly
- start customer/contact lifecycle redesign
- add speculative CRM features
- add batch tooling
- change governance rules beyond fixing summary truth and adding flagging UI
- weaken any existing block rules

REQUIRED TEST CASES

TC-001
Review a demo-flagged contact whose parent customer is not demo-flagged but is shared with live data.
Expected:
- governance summary/UI shows protected/shared state truthfully
- no false “isolated” message
- backend enforcement remains aligned

TC-002
As Owner/Admin, flag a customer as demo/test through the UI.
Expected:
- no API-only workaround needed
- state updates correctly
- governance review reflects the flag

TC-003
As Owner/Admin, flag a contact as demo/test through the UI.
Expected:
- no API-only workaround needed
- state updates correctly
- governance review reflects the flag

TC-004
As non-high-level user, verify customer/contact demo-flag controls are not accessible.
Expected:
- no regression in role gating

TC-005
Verify existing CRM governance protections still hold.
Expected:
- shared-live customer/contact records remain blocked from unsafe archive/delete
- Xero-linked protections remain intact

TC-006
Verify Settings > System still shows:
- truthful read-only environment information
- role-gated governance section
- no fake environment selector
Expected:
- no regression

DELIVERABLES REQUIRED

Do not report complete unless you provide:
1. exact files changed
2. exact UI surfaces added/changed for customer/contact demo-flagging
3. exact contact isolation summary fix implemented
4. confirmation of role gating behavior
5. results of all required test cases
6. known remaining gaps

OUTPUT FORMAT REQUIRED

Return your completion report in this exact structure:

1. SUMMARY
- what was implemented
- what was intentionally not changed

2. FILES CHANGED
- full file list
- one-line purpose of each change

3. CONTACT ISOLATION SUMMARY FIX
- old behavior
- new behavior
- how summary/UI now aligns with backend enforcement

4. CUSTOMER / CONTACT FLAGGING UI
- exact surfaces changed
- exact Owner/Admin behavior
- exact non-high-level behavior

5. SAFETY MODEL PRESERVATION
- customer shared-live blocking
- contact shared-live blocking
- Xero protections
- role gating
- no regression statement

6. TEST RESULTS
- TC-001 result
- TC-002 result
- TC-003 result
- TC-004 result
- TC-005 result
- TC-006 result

7. KNOWN GAPS
- exact remaining limitations
- what should move to a later phase rather than being forced here

FINAL EXECUTION STANDARD

Treat this as a live enterprise-platform continuation, not a casual reset.
Preserve the architecture.
Preserve the governance model.
Prefer archive over delete.
Allow delete only for explicit test/demo data where all safeguards pass.
Protect shared live CRM records.
Report truthfully and exactly.