```text IMPLEMENTATION MODE — FOLLOW EXACTLY You are implementing a focused Item Library enhancement pass in an active production-style quoting/manufacturing system. Operate as an executor, not a planner. NON-NEGOTIABLE RULES 1. Do NOT reinterpret this brief into a different architecture. 2. Do NOT redesign quote lifecycle, revision flow, snapshot flow, or quote template architecture. 3. Do NOT replace the current Library → Quote Builder → Estimate → Quote flow. 4. Do NOT add unrelated schema fields, endpoints, or systems. 5. Do NOT turn Role into free text on profile items. 6. Do NOT introduce a second unrelated image system for profiles if an existing image-storage pattern can be reused safely. 7. Preserve the business rule that profile Role must be a controlled, admin-manageable dictionary value. 8. Focus only on: - editable profile role dictionary - profile reference images - library detail visibility - cut sheet / report readiness using those images and roles ARCHITECTURE GUARDS Library-backed manufacturing/reference data → Quote Builder / internal selection logic → Estimate / Quote generation where relevant → manufacturing outputs / cut sheets / reports Profile Role Dictionary → controlled dropdown source for profile.role Profile Reference Image → optional asset attached to profile library entries → usable in library detail view, cut sheets, and reports Do NOT collapse these concerns into the quote template system. SCHEMA / API RESTRICTIONS Only add the minimum schema/config/storage support strictly required for: - editable profile role dictionary - optional profile reference images - library detail usage - cut sheet/report visibility readiness Do NOT add unrelated fields. Do NOT add unrelated endpoints unless required by the existing library/settings save/load patterns. Do NOT change payload shapes outside the scoped task unless required and documented. OUTPUT FORMAT REQUIRED At the end provide: 1. Files changed 2. Exact behavior changes made 3. Any schema/API/config/storage changes made 4. Exact profile role dictionary behavior 5. Exact profile reference image behavior 6. How library detail view was updated 7. How cut sheets / reports now use or are prepared to use role + image data 8. Any migration/backfill actions performed 9. Any risks or limitations remaining 10. Confirmation architecture guards were preserved NOW IMPLEMENT THE FOLLOWING TASKS EXACTLY: OBJECTIVE Implement: 1. an editable Profile Role Dictionary 2. optional Profile Reference Images 3. library detail visibility for those images 4. internal cut sheet / report support for role + image reference data Business intent: - Profile roles must remain controlled dropdown values - Admins must be able to add/manage the role list - Each profile item can optionally store a reference image - The image should be visible in the library edit/detail UI - The same role/image data should be usable in cut sheets and internal reports -------------------------------------------------- T139 — Add a managed Profile Role Dictionary -------------------------------------------------- Problem: The current profile Role dropdown is too rigid and appears to be hardcoded. The business needs to add and manage more role names over time. Required implementation: 1. Create a managed Profile Role Dictionary as the source of truth for profile.role. 2. Keep Role as a controlled dropdown, NOT free text. 3. Admins must be able to: - view existing role options - add a new role option - rename/edit a role option safely - retire/remove an unused role option where safe 4. If deletion is risky because a role is already in use: - prevent deletion or require safe reassignment - do NOT silently orphan profile records 5. Existing profile entries must continue to work and map cleanly to the role dictionary. Acceptance criteria: - Role dropdown is no longer hardcoded in code - Role options come from a managed dictionary - admins can maintain the role list safely - profile entries still use controlled dropdown selection only -------------------------------------------------- T140 — Add optional Profile Reference Image support -------------------------------------------------- Problem: Profile entries need a reference image for identification, admin accuracy, and future manufacturing/report outputs. Required implementation: 1. Add optional reference image support to profile library entries. 2. Each profile item should be able to store: - no image - uploaded image and/or safe URL-backed image depending on the current app’s image patterns 3. The image must be visible: - in the profile edit modal - in a detail/preview context within the library UI 4. Reuse an existing safe image-storage approach if practical. 5. Do NOT introduce a brittle one-off image approach if the app already has a better asset pattern. Acceptance criteria: - profile entries can store a reference image - image can be uploaded/associated and previewed - image persists correctly -------------------------------------------------- T141 — Update the Item Library profile edit flow -------------------------------------------------- Problem: The current Edit Profile modal needs to support the new managed Role dictionary and reference image. Required implementation: 1. Update the Profile edit modal so: - Role uses the managed dropdown source - reference image can be added/changed/removed - preview is visible in the edit experience 2. Keep the rest of the existing profile editing flow intact: - mould number - kg/m - price - length formula - family groups - division scope 3. Keep the UI professional and easy to use. Acceptance criteria: - profile editing supports managed roles and image preview cleanly - no loss of existing profile editing behavior -------------------------------------------------- T142 — Add profile image visibility in library table/detail view -------------------------------------------------- Problem: The reference image should not only exist in the edit modal; it should be useful in the library UI. Required implementation: 1. Add a detail/preview visibility path in the library for profile images. 2. Minimum acceptable implementation: - image thumbnail or indicator in the table - hover/detail expansion or inline preview 3. Better implementation if safely achievable: - image visible in a detail row / side panel / expanded item detail 4. Keep the table readable. Do NOT clutter the main library table excessively. Acceptance criteria: - users can see that a profile has a reference image - users can open/view the image in the library context - library remains readable and professional -------------------------------------------------- T143 — Prepare cut sheets and internal reports to use role + image data -------------------------------------------------- Problem: Profile reference data should support downstream manufacturing/reporting use, not just library admin. Required implementation: 1. Ensure the profile role and reference image data are available to internal cut sheet / report generation paths. 2. At minimum, wire the data so internal reports/cut sheets can include: - profile role - reference image if present 3. If the current cut sheet/report output is not yet fully implemented in-app, do the smallest safe preparation so the data is available in the relevant model/output path and document exactly what is now enabled vs deferred. 4. Do NOT force these reference images into customer-facing quotes unless already intended. Acceptance criteria: - role + image are usable in internal manufacturing/report contexts - the system is prepared for cut sheet/report use without architectural hacks -------------------------------------------------- T144 — Handle migration/backfill safely -------------------------------------------------- Problem: Existing profiles already have role values and no reference images. Required implementation: 1. Safely migrate or seed existing role values into the managed role dictionary. 2. Preserve all existing profile.role assignments. 3. Existing profiles without images should continue to work normally. 4. Do NOT break existing library/profile records. Acceptance criteria: - existing profile role values remain valid - role dictionary includes current in-use roles - no existing profile records are broken -------------------------------------------------- T145 — Keep this pass tightly scoped -------------------------------------------------- Do NOT: - redesign the quote template builder - redesign quote lifecycle - add customer portal/email logic - build a full DAM/media management system - make Role free text - rebuild the entire Item Library UI from scratch This is: - managed Profile Role Dictionary - Profile Reference Images - library detail visibility - cut sheet/report readiness -------------------------------------------------- SUCCESS CRITERIA -------------------------------------------------- After this pass: 1. Profile Role is controlled by an editable managed dictionary. 2. Admins can maintain the role list safely. 3. Profile entries can store and preview reference images. 4. Library UI shows image presence and supports viewing it. 5. Role + image data are available for internal cut sheets/reports. 6. Existing profile data remains intact and valid. 7. The system stays structured and future-ready for manufacturing workflows. END OF IMPLEMENTATION ```