STEELIQ MASTER HANDOVER STACK — CORRECTION PROMPT

AGENT CONTROL HEADER — MUST FOLLOW EXACTLY

You are working on an existing production-oriented system. You must implement exactly what is requested and preserve the current architecture.

Non-negotiable rules:
- Do not redesign the architecture.
- Do not change the quote lifecycle without explicit instruction.
- Do not replace or bypass the QuoteDocument pipeline.
- Do not merge snapshot state with live editable state.
- Do not introduce schema creep outside the requested phase.
- Do not add unapproved endpoints.
- Do not perform hidden refactors.
- Do not make speculative improvements that were not requested.
- Do not rename, relocate, or restructure major modules unless explicitly instructed.
- Do not “clean up” by changing architecture.
- Extend the current system incrementally and safely.
- If you believe a structural change is required, stop and explain why before making it.

==================================================
CURRENT ISSUE
==================================================

Stacker Door does not currently expose the same panel-selector controls as Sliding Door.

The user has confirmed the desired behaviour by reference to the existing Sliding Door UI.

Current Sliding Door behaviour:
- FIX
- AWN
- SLD
- left arrow
- right arrow

This same selector/control pattern is required for Stacker Door.

This is not a new design request.
This is a parity/fix request:
- review the existing Sliding Door implementation
- apply the same logic/pattern to Stacker Door where appropriate

==================================================
THIS PHASE — EXACT SCOPE
==================================================

Implement only this fix:

1. Bring Stacker Door panel controls into parity with Sliding Door panel controls

Do NOT broaden scope.

==================================================
REQUIRED BEHAVIOUR
==================================================

For Stacker Door, panel/row controls must support the same options/pattern already used in Sliding Door:

- FIX
- AWN
- SLD
- left arrow
- right arrow

Required result:
- Stacker Door builder UI shows the missing SLD + direction-arrow controls where applicable
- saved config state persists correctly
- drawing/preview updates correctly
- any downstream summary/spec logic using this state remains consistent

==================================================
IMPLEMENTATION DIRECTION
==================================================

Before changing code:
1. Inspect the current Sliding Door panel selector implementation
2. Identify the exact shared logic / component / conditional that provides:
   - FIX
   - AWN
   - SLD
   - left/right arrows
3. Compare that with the Stacker Door path
4. Explain exactly why Stacker Door is missing the SLD + arrow controls
5. Then implement the minimum safe parity fix

Important:
- do not invent a new selector model
- reuse the existing Sliding Door logic/pattern where safe
- do not redesign the builder architecture

==================================================
REGRESSION REQUIREMENTS
==================================================

Must verify:
- Sliding Door still behaves exactly as before
- Stacker Door now shows the same control pattern where intended
- SLD + left/right arrow state saves correctly
- preview/drawing reflects the selected direction correctly
- no break to other multi-panel configurations
- no break to quote builder save/update flow

==================================================
MANDATORY HANDOVER FORMAT
==================================================

Return final handover in this exact structure:

1. Files changed
2. Planned tasks summary
3. Exact root cause found
4. Behaviour changes
5. Schema/API changes
6. UI changes
7. Architecture verification
8. Risks/limitations
9. Updated current system state
10. Updated recommended next tasks
11. Regression checks

Do not claim completion unless Stacker Door visibly supports the same selector pattern as Sliding Door and is verified in the live builder path.