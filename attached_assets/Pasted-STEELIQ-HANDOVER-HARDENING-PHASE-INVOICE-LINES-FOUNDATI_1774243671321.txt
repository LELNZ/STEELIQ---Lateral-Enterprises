STEELIQ HANDOVER HARDENING PHASE — INVOICE LINES FOUNDATION REVIEW + STARTUP SAFETY CLARIFICATION (NO NEW IMPLEMENTATION PHASE)

AGENT CONTROL HEADER — MUST FOLLOW EXACTLY

You are working on an existing live, production-oriented system. This phase is NOT a new feature phase. It is a narrow clarification, validation, and handover-hardening phase for the recently completed invoice-lines foundation work.

Non-negotiable rules:
- Do not start a new implementation phase.
- Do not widen scope.
- Do not redesign the architecture.
- Do not redesign the quote model.
- Do not implement multi-line invoice UI in this phase.
- Do not change customer quote PDF generation.
- Do not change Xero payload structure in this phase.
- Do not weaken Xero protections.
- Do not weaken governance, lifecycle, division security, archive/delete behavior, or existing invoice safeguards.
- Do not perform hidden refactors.
- Do not introduce speculative improvements outside the explicit scope below.
- Treat current repo truth as the source of truth, not assumptions from previous prompts.
- If repo truth differs from prior handover wording, report the mismatch explicitly and follow repo truth.
- Do not describe standalone admin/manual invoice creation as already implemented unless you identify an actual current route/UI for quote-independent invoice creation.
- Do not forget the reported live app freeze after inactivity; it remains out of scope for this phase unless directly encountered, but it must remain listed in open risks.
- Do not forget subcontractor PDF export branding/polish remains an adjacent open item, but it is out of scope for this phase.
- Do not make broad claims like “no regressions” or “completely unchanged” unless you specify what was actually verified.

PHASE PURPOSE

The prior phase reportedly implemented:
- invoice_lines table
- storage methods
- dual-write on invoice creation
- read endpoint
- startup backfill for historical invoices

This follow-up phase exists only to:
1. verify and report exact repo truth
2. harden the handover report
3. clarify startup/backfill safety
4. confirm what is preserved
5. confirm what remains intentionally incomplete

This is a documentation/validation phase, not a build phase.

STRICT SCOPE

You must inspect the repo and return a strict, enterprise-grade completion report for the invoice-lines foundation phase.

You must answer precisely:
- exactly what changed
- exactly where it changed
- exactly how startup backfill is wired
- whether startup safety is acceptable
- exactly what still remains single-line
- exactly what remains unchanged in Xero/invoice operational behavior

You may make tiny wording/documentation-only cleanups if absolutely necessary for truthful handover, but do not change functional behavior unless there is a critical correctness bug in the reported implementation and you stop and explain first.

REQUIRED REVIEW TARGETS

Review at minimum all relevant files involved in the reported invoice-lines phase, including wherever applicable:
- shared/schema.ts
- server/storage.ts
- server/routes.ts
- any invoice line backfill file(s)
- any server startup/bootstrap file(s) that invoke backfill
- any touched invoice-related client files, if changed
- any migration file(s), if used

PRIMARY QUESTIONS TO ANSWER

A. SCHEMA TRUTH
Report exact invoice_lines shape:
- fields
- types
- defaults
- nullability
- constants/types added
- any indexes/constraints
- whether the schema was kept intentionally minimal

B. STORAGE / ROUTE TRUTH
Report exact additions:
- storage interface methods
- storage implementation methods
- route(s) added
- how invoice lines are read
- how lines are sorted
- whether any write/update/delete routes were added or not

C. DUAL-WRITE TRUTH
Report exact current behavior:
- where POST /api/invoices performs dual-write
- what lineType is used for each current invoice type
- what description is copied
- what quantity is written
- what unitAmount is written
- what lineAmountExclGst is written
- how variationId is populated
- whether sourceContext is used and what it contains
- what happens if line creation fails
- whether the invoice still succeeds if the line write fails
- how that risk is justified in this stage

D. BACKFILL / STARTUP SAFETY TRUTH
This is critical. You must answer exactly:
- what file contains the backfill logic
- what file invokes it
- whether it runs before or after the server starts listening
- whether it can delay server readiness
- whether it runs on every startup
- why that is considered safe or not safe
- what happens as invoice count grows
- whether the design violates the startup-path guardrail
- whether this is only acceptable because current invoice volume is low
- whether you recommend a later migration to a better mechanism
- whether the backfill is idempotent and how that is guaranteed

E. COMPATIBILITY TRUTH
State explicitly whether these remain behaviorally unchanged in current repo truth:
- invoice list page
- invoice detail page
- quote detail invoice section
- invoice allocation guards
- variation invoicing protections
- retention controls
- invoice status lifecycle
- Xero push payload
- Xero payment sync
- governance/demo/division/archive behavior

F. KNOWN LIMITS
State explicitly that, after this phase:
- there is still no multi-line invoice authoring UI
- there is still no multi-line Xero push
- invoice header remains authoritative
- line data is still foundational/shadow-stage
- the invoice detail page still behaves operationally as a single-line invoice model

NON-GOALS

Do NOT:
- start implementing the next phase
- add multi-line invoice editing
- change Xero line generation
- redesign invoice totals
- change quote architecture
- add standalone manual/admin invoice composition
- refactor unrelated modules
- soften or hide any startup/backfill caveats
- overstate verification that did not occur

REQUIRED DELIVERABLES

Return your report in this exact structure:

1. SUMMARY
- what was implemented in the prior invoice-lines foundation phase
- what was intentionally not implemented
- confirm header invoice amounts remain authoritative
- confirm Xero payload generation is unchanged
- confirm invoice UI remains operationally single-line

2. EXACT FILES CHANGED
- full file list only
- one-line purpose for each file

3. SCHEMA RESULT
- exact invoice_lines table fields
- defaults
- nullability
- constants/types added
- indexes/constraints added
- why it was kept minimal

4. SERVER / STORAGE RESULT
- exact storage methods added
- exact route(s) added
- exact place where POST /api/invoices dual-write occurs
- exact behavior if invoice line creation fails
- exact logging/error handling used

5. BACKFILL / STARTUP SAFETY RESULT
Answer all of these explicitly:
- exactly where startup backfill is invoked
- whether it runs before or after server listen
- whether it can delay server readiness
- why running on every startup is safe or unsafe
- what happens as invoice count grows
- why this does or does not violate the startup-path guardrail
- whether this is only acceptable because current invoice volume is low
- whether a better later migration pattern is recommended
- how idempotency is guaranteed

6. COMPATIBILITY RESULT
Confirm specifically whether these remain unchanged in behavior:
- invoice list page
- invoice detail page
- quote detail invoice section
- allocation guards
- variation invoicing protections
- retention controls
- Xero push payload
- Xero payment sync
- governance/demo/division/archive behavior

7. KNOWN LIMITS AFTER THIS PHASE
Must explicitly state:
- no multi-line invoice authoring UI yet
- no multi-line Xero push yet
- header remains authoritative
- line data is still foundational/shadow-stage
- current invoice detail still behaves as a single-line operational model

8. RECOMMENDED NEXT PHASE
- one narrow next phase only
- no branching roadmap

9. OPEN RISKS / WATCH ITEMS
Must include:
- reported live app freeze after inactivity
- subcontractor PDF export branding/polish still potentially incomplete
- any startup/backfill caveat discovered
- any temporary dual-write inconsistency risk

FINAL EXECUTION STANDARD

Treat this as a live enterprise-platform continuation, not a casual reset.
Preserve architecture discipline.
Do not drift into a new build phase.
Return an exact, conservative, repo-truth handover report for the invoice-lines foundation work.
Do not claim more than what the repo actually proves.