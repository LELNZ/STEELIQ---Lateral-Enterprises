IMPLEMENTATION MODE — FOLLOW EXACTLY

You are implementing a scoped change in an active production-style software project.

Operate as an executor, not a planner.

NON-NEGOTIABLE RULES
1. Do NOT reinterpret this brief into a different architecture.
2. Do NOT redesign existing flows, schemas, or component structure unless explicitly instructed.
3. Do NOT add new schema fields, tables, API endpoints, route patterns, or background behaviors unless explicitly specified in this brief.
4. Do NOT introduce speculative improvements outside scope.
5. Do NOT rename core models, lifecycle terms, or pipeline stages unless explicitly instructed.
6. Do NOT replace the documented Quote/Revision/QuoteDocument flow.
7. Do NOT silently change UX meaning through label or behavior drift.
8. If something appears missing, ambiguous, or risky, stop and ask before changing the design.
9. Prefer enhancement over replacement.
10. Preserve backward compatibility unless the brief explicitly allows a breaking change.

ARCHITECTURE GUARDS
Estimate / Project
→ Quotes
→ Revisions
→ Snapshot
→ QuoteDocument
→ Quote Preview
→ Quote Renderer
→ PDF Export

Do NOT bypass QuoteDocument.
Do NOT collapse live item state with snapped revision state.
Do NOT move rendering responsibilities into Quote Builder.
Do NOT move photo/media controls into Visible Specs.

SCHEMA / API RESTRICTIONS
Do NOT add new schema fields unless explicitly instructed.
Do NOT add new endpoints unless explicitly instructed.
Do NOT change payload shapes outside the scoped task unless required and documented.

IMPLEMENTATION BEHAVIOR
Implement the tasks exactly as written.
Use existing patterns and components.
Make the smallest safe changes needed.

OUTPUT FORMAT REQUIRED
At the end provide:
1. Files changed
2. Exact behavior changes made
3. Any schema/API changes made
4. Any risks or limitations remaining
5. Confirmation architecture guards were preserved

NOW IMPLEMENT THE FOLLOWING TASKS EXACTLY:

Objective:
Refine quote preview usability and item schedule layout now that renderer architecture is stable.

Context:
The current snapshot-based quote workflow is working correctly:
- item photo/spec changes only appear after updating/regenerating the quote revision
- preview rendering is now correctly distinguishing renderable vs missing media

However, real usage has shown current UX friction and layout issues:
- updating a quote after media/spec changes feels too cumbersome
- quote preview still shows “view more” behavior in item specs
- long spec names do not wrap cleanly
- spec area is not yet presented as a polished two-column quote layout

This milestone is a focused UX/layout refinement pass, not an architecture redesign.

Required implementation tasks:

1. Improve quote update/regeneration UX clarity without changing snapshot semantics.
   Requirements:
   - keep Quote/Revision/Snapshot architecture intact
   - make it clearer in the UI that quote output reflects the current revision snapshot
   - make the update/regenerate action easier to understand and easier to reach where appropriate
   - reduce user confusion around why newly included photos/specs do not appear until the quote is updated
   - do not turn preview into a live unsnapshotted view

2. Refine item schedule/spec layout in quote preview.
   Requirements:
   - long spec names must wrap cleanly
   - spec rows must remain readable in print/preview
   - move toward a clean two-column spec/value layout where appropriate
   - preserve current data and meaning
   - do not create visual overflow or truncation issues

3. Remove or redesign the current “view more” / chevron behavior for quote item specs in preview if it harms readability.
   Requirements:
   - quote output should feel like a professional customer-facing document
   - avoid interactive UI patterns that make the printed/exported quote feel incomplete
   - if collapsing is still necessary for extreme cases, use a cleaner quote-appropriate approach and justify it

4. Keep media and spec rendering consistent.
   Requirements:
   - drawings/photos continue to render correctly
   - spec visibility rules remain intact
   - no regression to current renderer pipeline

5. Keep this milestone focused.
   Do NOT:
   - redesign the whole quote system
   - add theme editors
   - add presentation mode switching yet
   - add PDF export
   - redesign backend quote lifecycle
   - break revision immutability

Acceptance criteria:
- quote preview remains end-to-end functional
- users can better understand when a quote must be updated to capture changes
- spec layout is cleaner and more readable
- long names wrap properly
- the current “view more” behavior is removed or significantly improved for customer-facing preview
- architecture guard remains intact

At the end provide:
1. Files changed
2. Exact behavior changes made
3. Any schema/API changes made
4. Any risks or limitations remaining
5. Confirmation architecture guards were preserved